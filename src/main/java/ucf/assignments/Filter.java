/*
 *  UCF COP3330 Fall 2021 Assignment 4 Solution
 *  Copyright 2021 Cameron Parrish
 */
package ucf.assignments;

import java.util.ArrayList;
import java.util.List;

public class Filter {

    public static ToDoListMain.ToDoList AllItems(ToDoListMain.ToDoList CurrentList){
        //Return CurrentList
        return null;
    }

    public static ToDoListMain.ToDoList CompleteItems(ToDoListMain.ToDoList CurrentList){
        //List<ToDoListMain.ToDoList> FilterList = new ArrayList<ToDoListMain.ToDoList>();
        // Iterate through current list to check when an object inside's status is complete
        // for i=0; i<CurrentList.size(); i++
        // using CurrentList.get(i) (you get the object)
        // get obj status
        // if status == complete
        // FilterList.Add()
        // Return FilterList

        return null;
    }

    public static ToDoListMain.ToDoList InCompleteItems(ToDoListMain.ToDoList CurrentList){
        //List<ToDoListMain.ToDoList> FilterList = new ArrayList<ToDoListMain.ToDoList>();
        // Iterate through current list to check when an object inside's status is incomplete
        // for i=0; i<CurrentList.size(); i++
        // using CurrentList.get(i) (you get the object)
        // get obj status
        // if status == incomplete
        // FilterList.Add()
        // Return FilterList
        return null;
    }
}
